name: Basic CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Print message
        run: echo "CI Started"

      - name: Show current directory
        run: pwd

      - name: List files
        run: ls
